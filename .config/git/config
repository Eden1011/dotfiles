[user]
	name = Krzysztof Główka
	email = krzysztof.glowka@pm.me
	signingkey = E116C081E47E7C97

[alias]
	co = checkout
	sw = switch
	br = branch
	brv= branch -vv
	ci = commit
	st = status
	mg = merge
	mgtool = mergetool
	pl = pull
	pu = push
	fe = fetch
	fent = fetch --no-tags
	re = remote
	rb = rebase
	sh = show
	cp = cherry-pick
	db = describe
	ac = archive
	sl = shortlog
	rl = reflog
	rlv= log -g
	sa = stash
	cn = clean
	rs = reset
	rt = restore
	rv = revert
	cat= cat-file
	tree= ls-tree
	files= ls-files
	amend = commit --amend 
	whitespace = diff --check
	unstage = "restore --staged"
	last = "log -1 --patch --stat HEAD"
	graph = "log --all --oneline --decorate --graph"
	publish = "commit -a"
	re3 = rerere
	bl = blame
	bi = bisect
	sbm = submodule
	sbt = subtree
	rp = rev-parse
	sbmdiff = "!git diff && git submodule foreach 'git diff'"
	sbmpu = "push --recurse-submodules=on-demand"
	vc = verify-commit
	vt = verify-tag

[rerere]
	enabled = true
	autoUpdate = true

[help]
	autocorrect = prompt

[credential]
	helper = cache

[merge]
	tool = kdiff3
	conflictstyle = diff3

[submodule]
	recurse = true

[commit]
	gpgsign = true
	verbose = true
	template = ~/.gitmessage

[core]
	preloadindex = true  
	editor = nvim
	excludesfile = ~/.gitignore_global
	autocrlf = input
	whitespace = "blank-at-eol,blank-at-eof,space-before-tab,tabwidth=2"

[blame]
	coloring = highlightRecent

[diff]
	renames = copies
	submodule = log

[init]
	defaultBranch = main 

[status]
	branch = true
	showStash = true
	showUntrackedFiles = all  
	submoduleSummary = true 

[pager]
	branch = false 

[push]
	default = current  
	recurseSubmodules = check

[pull]
	default = current

[rebase]
	missingCommitsCheck = error 

[branch]
	sort = -committerdate

[tag]
	gpgsign = true
	sort = -taggerdate

[interactive]
	singlekey = true

[url "git@github.com:"]
	insteadOf = "gh:"

[url "git@github.com:Eden1011/"]
	insteadOf = "ed:"

[url "git@gitlab.com:"]
	insteadOf = "gl:"

[filter "lfs"]
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
